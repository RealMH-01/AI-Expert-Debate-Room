/**
 * Gemini Provider - Legacy Compatibility Layer
 *
 * Round 7: Delegates to GeminiAdapter in adapters/ directory.
 * Kept for backward compatibility with existing imports.
 */

export { GeminiAdapter as GeminiProvider } from './adapters'
